import React from 'react';

import styles from './CastMember.module.scss';

const CastMember = ({
    name,
    pictureLink
}) => {
    return (
        <div>

        </div>
    );
};

export default CastMember;